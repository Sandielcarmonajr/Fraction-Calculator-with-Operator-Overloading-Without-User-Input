# Fraction-Calculator-with-Operator-Overloading-Without-User-Input
In this assignment, I created a C++ program that simulates a basic fraction calculator.  The program uses a Fraction class with operator overloading to perform addition,  subtraction, multiplication, and division between fractions. 
